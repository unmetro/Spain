version: '3.8'

services:
  
  books:
    image: books-micro
    conatiner_name: books
    network:
      - library-soa
    ports:
      - "8085:5000"

  loans:
    image: loans-micro
    container_name: loans
    network:
      - library-soa
    ports:
      - "8086:5000"

  users:
    image: users-micro
    container_name: users
    network:
      - library-soa
    ports:
      - "8087:5000"
